name: Release
on:
  workflow_dispatch:
    branches:
      -main
jobs:
  release:
    name: Release
    permissions:
      packages: write
      contents: read
    uses: ./.github/workflows/common.yml
    with:
      release: true
    secrets:
      REGISTRY_PAT: ${{secrets.REGISTRY_PAT}}
